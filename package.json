{
  "scripts": {
    "build": "rimraf dist && tsc --build tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@types/puppeteer": "^7.0.4",
    "dotenv": "^16.0.3",
    "joi": "^17.7.0",
    "puppeteer": "^19.4.1",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
